PORT=4001

# Database persistente (Turso / libSQL).
# Senza queste due variabili il server usa un file SQLite locale che va
# perso a ogni nuovo deploy. Con Turso i dati restano salvati tra i deploy.
# Come ottenere i valori: vedi DATABASE.md.
DATABASE_URL=
DATABASE_AUTH_TOKEN=
